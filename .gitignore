# =================================================================
# UNREALITY3D TEMPLATE REPOSITORY .GITIGNORE - UNITY 6+ WEBGL
# =================================================================
# This file serves multiple purposes:
# 1. Template repo development (includes WebGL build target)
# 2. Creator deployments (modified by pipeline for WebGL hosting)
# 3. Chunk file handling (allows temporary .part* files)
# =================================================================

# Unity Development Files (ALWAYS IGNORED)
# -----------------------------------------------------------------
/[Ll]ibrary/
/[Tt]emp/
/[Oo]bj/
/[Ll]ogs/
/[Uu]ser[Ss]ettings/
/[Mm]emoryCaptures/

# Unity Project Files (ALWAYS IGNORED)
# -----------------------------------------------------------------
# Never ignore Asset meta data
!/[Aa]ssets/**/*.meta

# Blender backup files
*.blend1
*.blend1.meta

# Packed Addressables
/[Aa]ssets/[Aa]ddressable[Aa]ssets[Dd]ata/*.*.bin*

# Temporary auto-generated Android Assets
/[Aa]ssets/[Ss]treamingAssets/aa.meta
/[Aa]ssets/[Ss]treamingAssets/aa/*

# TextMeshPro Generated Assets (IGNORE - AUTO-REGENERATED)
# -----------------------------------------------------------------
# TextMeshPro font assets are auto-generated and should not be version controlled
# Unity will recreate these automatically when the project is opened
[Aa]ssets/TextMesh*Pro/
[Aa]ssets/**/TextMesh*Pro/
**/LiberationSans*SDF*.asset
**/LiberationSans*SDF*.asset.meta
**/*SDF*.asset
**/*Atlas*.asset
# Allow TMP Settings and Essential Resources (commented out means ALLOW)
# ![Aa]ssets/TextMeshPro/Resources/TMP*Settings*.asset
# ![Aa]ssets/TextMeshPro/Resources/Fonts*.asset

# Development IDE Files (ALWAYS IGNORED)
# -----------------------------------------------------------------
# Visual Studio cache files
*.suo
*.user
*.userosscache
*.sln.docstates
.vs/

# Unity autogenerated solution and project files
ExportedObj/
.consulo/
*.csproj
*.unityproj
*.sln
*.tmp
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# JetBrains Rider
/[Aa]ssets/Plugins/Editor/JetBrains*

# Gradle cache
.gradle/

# Unity meta files for binaries
*.pidb.meta
*.pdb.meta
*.mdb.meta

# System Files (ALWAYS IGNORED)
# -----------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Development Tools (ALWAYS IGNORED)
# -----------------------------------------------------------------
# Firebase development
.firebase/
firebase-debug.log*
firebase-debug.*.log*
node_modules/
.env

# Log files
*.log

# Unity crash reports
sysinfo.txt

# Build Outputs (CONDITIONAL - MODIFIED BY DEPLOYMENT PIPELINE)
# -----------------------------------------------------------------
# NOTE: These patterns are REMOVED during WebGL deployment
# Template development: ignores build outputs
# Creator deployment: allows Build/ folder for GitHub Pages hosting

# Unity build directories (disabled during WebGL deployment)
/[Bb]uilds/
##WEBGL_DEPLOY_IGNORE_START##
/[Bb]uild/
Build/
##WEBGL_DEPLOY_IGNORE_END##

# Other build outputs (always ignored)
*.apk
*.unitypackage
*.aab

# Unreality3D Configuration (ALWAYS IGNORED)
# -----------------------------------------------------------------
# API keys and config files (contains sensitive data)
u3d-config.json
*.config.json
firebase-config.json
*-api-keys.*

# Unity Settings (TEMPLATE-SPECIFIC CONFIGURATION)
# -----------------------------------------------------------------
##WEBGL_DEPLOY_IGNORE_START##
# CRITICAL CHANGE: ALLOW ProjectSettings to be committed for WebGL template
# This ensures users get WebGL build target when they clone the template
# !ProjectSettings/ProjectSettings.asset  # ALLOW - Template needs WebGL as default

# HOWEVER: Ignore specific settings that can cause conflicts between machines
ProjectSettings/Packages/
ProjectSettings/ProjectVersion.txt
ProjectSettings/VFXManager.asset

# CRITICAL: Allow creator PayPal configuration to be committed
!Assets/U3D_SDK/Resources/U3DCreatorData.asset
# CRITICAL: Allow U3D prefabs to be committed
!Assets/U3D/Prefabs/**/*
# CRITICAL: Allow U3D assets to be committed  
!Assets/U3D/U3D_Assets/**/*.asset
##WEBGL_DEPLOY_IGNORE_END##

# Chunk File Handling (FOR LARGE FILE SUPPORT)
# -----------------------------------------------------------------
# IMPORTANT: Chunk files (.part*) are temporarily allowed during deployment
# They are automatically cleaned up after reassembly by GitHub Actions

# Allow chunk files during deployment process
# !*.part*  # Explicitly allow .part* files (commented = allowed)

# Backup directories created during chunk reassembly
.chunks_backup/
*/.chunks_backup/

# Chunk reassembly logs and temporary files
*.chunk.log
*.reassembly.tmp

# Development Dependencies (ALWAYS IGNORED)  
# -----------------------------------------------------------------
*.localization
crashlytics-build.properties

# GitHub Actions (INCLUDED IN TEMPLATE)
# -----------------------------------------------------------------
# The .github/workflows/ directory is included in the template
# and should be committed to provide chunk reassembly functionality
# !.github/workflows/reassemble-chunks.yml